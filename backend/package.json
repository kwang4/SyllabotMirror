{
  "name": "backend",
  "version": "1.0.0",
  "description": "To run docker for this, CMD + SHIFT + P then 'Docker Image: ...'",
  "main": "index.js",
  "scripts": {
    "start": "node ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
