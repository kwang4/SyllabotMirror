# Syllabot Web Dashboard

## Install instructions
* cd to /Dashboard Folder
* Run "npm i" to install dependencies
* Run "npm run dev" to run the dev localhost
* Navigate to http://localhost:3000 to see the page
