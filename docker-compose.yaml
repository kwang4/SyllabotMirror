services:
  proxy:
    build: ./proxy
    restart: unless-stopped
    volumes:
      - ./proxy/default.conf.template:/etc/nginx/templates/default.conf.template
    ports:
      - "443:443"
      - "80:80"

  backend:
    build: ./backend
    restart: unless-stopped
    volumes:
      - ./backend/src:/app/src
    environment:
      - PORT=80
    env_file: 
      - .env 